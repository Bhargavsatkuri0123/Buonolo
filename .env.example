# Only needed when running `npm run dev` directly against a separately-running
# backend/ (e.g. not through nginx). Leave empty for the nginx-proxied setup,
# where relative /api and /ws paths already resolve correctly.
VITE_API_BASE_URL=
VITE_GOOGLE_CLIENT_ID=
GEMINI_API_KEY=
